# Variáveis de ambiente (Render -> Environment)
# Sem elas, o sistema roda em modo arquivo local (db.json).
SUPABASE_URL=https://seu-projeto.supabase.co
SUPABASE_KEY=sua-service-role-key-aqui
PORT=3000
